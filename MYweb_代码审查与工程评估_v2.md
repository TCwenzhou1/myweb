# MYweb 代码审查与工程评估 v2

更新时间：2026-04-07
审查基线：当前本地 `main` 工作区实际代码状态
仓库：`TCwenzhou1/myweb`
线上域名：`https://www.tcwenzhou.site`

---

## 一、这份 v2 的定位

这份文档是对上一版评估的校正和升级。

上一版评估的大方向是对的，但有一部分结论混入了“实验分支/过渡实现”的判断，没有完全和当前 `main` 对齐。
这版的目标不是重复讲“组件很多”，而是把下面三件事同时说清楚：

1. 当前 `main` 真实处于什么状态
2. 你真正想要的目标态是什么
3. 从当前状态走到目标态，优先级应该怎么排

一句话先说结论：

**MYweb 值得继续做，而且已经具备鲜明产品方向；但当前最大问题已经不只是“工程收口慢”，而是 `/lab` 的能力目标和主分支实现之间存在明显落差。**

---

## 二、总体判断

### 综合评分

- 产品方向：`8.8 / 10`
- 视觉表达：`8.1 / 10`
- 当前主分支工程完成度：`6.7 / 10`
- 长期可维护性：`6.2 / 10`
- `/lab` 与“真正日语全词库系统”目标的一致度：`4.8 / 10`

### 总结判断

当前仓库已经不是一个普通作品集，而是一个正在向“个人产品站 + 学习产品实验室”发展的项目。
问题不在于方向错误，而在于：

- 线上主分支还保留着明显的过渡实现
- 代码库里“正式实现 / 旧实现 / 过渡实现”边界不清
- `/lab` 的真正目标已经升级成“日语全词库系统”，但 `main` 仍停留在“前端可用学习页 + 客户端整包词库”的阶段

这意味着现在最该做的事，不是继续堆页面，而是：

**把 `/lab` 从“能演示的学习页”推进为“真正可扩展的词库系统基础设施”，并同步收干净主实现。**

---

## 三、当前 `main` 的真实状态

这一节只描述当前主分支已存在、能在本地看到的实现，不把实验分支内容算进来。

### 1. 首页入口仍然是旧命名链路

当前首页入口 [app/page.tsx](/E:/桌面/项目/codex项目/MYweb/app/page.tsx) 使用的是：

- `HeroCards`
- `HomeOverviewV2`

这说明当前 `main` 的首页还没有完成主实现命名收口。
上一版文档里如果默认把 `HomeHeroClean` 视为正式主实现，那对当前 `main` 来说并不成立。

### 2. `/lab` 当前仍是客户端整包词库模式

当前 `/lab` 页面入口 [app/lab/page.tsx](/E:/桌面/项目/codex项目/MYweb/app/lab/page.tsx) 只是直接渲染 [LabPageClient.tsx](/E:/桌面/项目/codex项目/MYweb/app/lab/LabPageClient.tsx)。
而 [LabPageClient.tsx](/E:/桌面/项目/codex项目/MYweb/app/lab/LabPageClient.tsx) 当前主逻辑是：

- 客户端动态导入 `lib/vocabularyBank`
- 先用 `featuredLabEntries` 做兜底
- 在前端对整包词库做搜索、筛选、分页
- 收藏、复习、自测也都建立在本地已加载数据上

换句话说：

**当前 `main` 的 `/lab` 还不是服务端检索词库，而是“前端整包词库页”。**

### 3. `main` 上还没有服务端词库接口链路

当前主分支里并不存在这些正式文件：

- `app/api/lab/vocabulary/route.ts`
- `lib/server/loadLabVocabulary.ts`

这点很关键，因为它直接说明：

**当前 `main` 还没有真正的服务端词库搜索架构。**

### 4. 大词库仍然直接放在 TypeScript 大文件里

当前仓库词库文件体量已经很明显：

- [lib/vocabularyBank.ts](/E:/桌面/项目/codex项目/MYweb/lib/vocabularyBank.ts) 约 `1.51 MB`
- [lib/jlpt10kBank.ts](/E:/桌面/项目/codex项目/MYweb/lib/jlpt10kBank.ts) 约 `2.54 MB`
- [lib/jmdictBank.ts](/E:/桌面/项目/codex项目/MYweb/lib/jmdictBank.ts) 约 `619 KB`
- [lib/kaoyan3500Bank.ts](/E:/桌面/项目/codex项目/MYweb/lib/kaoyan3500Bank.ts) 约 `953 KB`

这类组织方式在“早期集成验证”阶段可以接受，但不适合作为“真正全词库系统”的长期形态。

### 5. 大组件问题仍然真实存在

当前主分支中几个关键文件体量如下：

- [app/lab/LabPageClient.tsx](/E:/桌面/项目/codex项目/MYweb/app/lab/LabPageClient.tsx) `946` 行
- [components/HeroCards.tsx](/E:/桌面/项目/codex项目/MYweb/components/HeroCards.tsx) `1196` 行
- [components/ProjectsContent.tsx](/E:/桌面/项目/codex项目/MYweb/components/ProjectsContent.tsx) `545` 行

这已经不是“小组件可接受偏大”，而是会直接影响：

- 功能继续扩展
- 命名收口
- 视觉重构
- Bug 排查成本

### 6. README 当前已经明显失真且存在编码异常

当前 [README.md](/E:/桌面/项目/codex项目/MYweb/README.md) 不只是旧，而是已经出现明显乱码。
这会同时影响：

- 仓库第一印象
- 公开可读性
- 项目可信度

这个问题应该上升为明确整改项，而不再只是“之后顺手重写”。

---

## 四、这次 v2 相比上一版的核心修正

### 修正 1：`/lab` 不能再被定义成“已有真实全词库能力”

上一版文档把 `/lab` 的评估重心放在“学习闭环已成型”上，这是对的，但不够了。
现在你的目标已经不是“有搜索、有词卡、有收藏”，而是：

**真正意义上的日语全词库系统。**

按这个标准看，当前 `main` 的 `/lab` 仍然只是：

- 带全量词库数据的前端学习页
- 不是服务端检索词典
- 不是可持续扩库的数据系统
- 不是最终意义上的全词库平台

### 修正 2：不能把实验分支里的改造当成 `main` 现状

之前有一部分判断默认了以下能力已经在主线存在：

- 服务端词库聚合
- `/api/lab/vocabulary`
- 首屏真实词库查询
- 按 ID 回查词条

但在当前 `main` 里，这些都还没有真正落下。

所以以后做文档、审查、排期时，必须严格区分：

- `main` 当前状态
- 已验证但未合入主线的方案
- 最终目标态

### 修正 3：当前最大 P0 已从“清理 V2 文件”升级成“词库系统架构升级”

组件命名混乱仍然是重要问题，但如果按你的目标优先级来排，真正的 P0 应该已经变成：

**把 `/lab` 从前端整包词库页升级成真正的服务端全词库系统。**

---

## 五、当前最核心的问题

## 1. `/lab` 与“真正的日语全词库系统”目标不匹配

### 当前情况

[LabPageClient.tsx](/E:/桌面/项目/codex项目/MYweb/app/lab/LabPageClient.tsx) 当前仍然是：

- 前端加载大词库
- 前端本地筛选
- 前端本地分页
- 前端本地把精选词卡和全量词库混在一个工作流里

### 为什么这是 P0

因为你现在对 `/lab` 的目标已经不是：

- 做一个能学词的页面

而是：

- 做一个真正意义上的日语全词库系统

这两者的工程要求完全不是一个量级。

### 结论

当前 `/lab` 最紧急的问题，不是 UI，而是架构。

**优先级：P0**

---

## 2. 词库数据的存放方式不适合长期扩库

### 当前情况

词库直接写在这些 `.ts` 文件里：

- [lib/vocabularyBank.ts](/E:/桌面/项目/codex项目/MYweb/lib/vocabularyBank.ts)
- [lib/jlpt10kBank.ts](/E:/桌面/项目/codex项目/MYweb/lib/jlpt10kBank.ts)
- [lib/jmdictBank.ts](/E:/桌面/项目/codex项目/MYweb/lib/jmdictBank.ts)
- [lib/kaoyan3500Bank.ts](/E:/桌面/项目/codex项目/MYweb/lib/kaoyan3500Bank.ts)

### 风险

- 继续扩库会进一步拖重构建和编辑体验
- 数据清洗、去重、合并逻辑难以规范化
- 不利于以后支持更多来源
- 不利于做服务端检索和分页

### 结论

这套方式可以作为“过渡数据源”，不能作为最终词库基础设施。

**优先级：P0**

---

## 3. 组件目录的主实现边界仍然混乱

### 当前情况

当前仓库里仍然并存这些实现：

- `HomeOverview.tsx`
- `HomeOverviewV2.tsx`
- `ProjectsContent.tsx`
- `ProjectsContentV2.tsx`
- `ReviewCard.tsx`
- `ReviewCardV2.tsx`
- `WordCard.tsx`
- `WordCardV2.tsx`
- `AboutContent.tsx`
- `AboutContentV2.tsx`
- `ContactContent.tsx`
- `ContactContentV2.tsx`
- `GamesContent.tsx`
- `GamesContentV2.tsx`

同时工作区里还出现了新的 `Enhanced`、`LabWordCard`、`LabReviewCard` 等未收口文件。

### 风险

- 自己改功能时容易改错入口
- 别人阅读仓库时很难知道线上到底用哪个
- 老文件越积越多，最终会拖垮维护效率

### 结论

这是非常明确的工程治理问题，且已经持续存在。

**优先级：P0 / P1**

---

## 4. README 和公开仓库入口已经影响专业观感

### 当前情况

[README.md](/E:/桌面/项目/codex项目/MYweb/README.md) 当前：

- 结构仍然是早期作品集说明
- 内容和当前站点方向不一致
- 存在编码乱码

### 结论

README 现在不是“可优化项”，而是“必须修复项”。

**优先级：P1**

---

## 5. 根目录仍然混入内容资产和临时文件

当前根目录可见：

- [203_考研日语_超全整合版_网站导入稿.md](/E:/桌面/项目/codex项目/MYweb/203_考研日语_超全整合版_网站导入稿.md)
- [japanese-lab-cn2000.zip](/E:/桌面/项目/codex项目/MYweb/japanese-lab-cn2000.zip)
- [build-output.txt](/E:/桌面/项目/codex项目/MYweb/build-output.txt)
- `dev-3010.err.log`
- `dev-3010.out.log`

### 结论

这些文件不一定没价值，但不应长期堆在仓库根目录。

**优先级：P1**

---

## 6. `useStudyStore` 仍然是“本地 MVP 状态层”

### 当前情况

[useStudyStore.ts](/E:/桌面/项目/codex项目/MYweb/lib/useStudyStore.ts) 当前特点：

- 从 `localStorage` 读取
- 整包写回 `localStorage`
- 有 `v4 / v3` 兼容意识
- 状态仍集中在单个对象里

### 判断

它对当前阶段是合理的，而且实现并不差。
但它还不是长期数据层，只能算“浏览器本地学习进度系统”。

### 结论

短期内可以保留，但要准备版本化和分层。

**优先级：P1**

---

## 7. `useSpeech` 的问题不是代码烂，而是路线天花板

### 当前情况

[useSpeech.ts](/E:/桌面/项目/codex项目/MYweb/lib/useSpeech.ts) 使用浏览器原生 `speechSynthesis`。

### 判断

这条路线适合：

- 先让 TTS 功能存在
- 低成本验证交互闭环

但不适合直接当作强一致、可控的核心体验依赖。

### 结论

保留可以，但应明确它是增强能力而不是底层强依赖。

**优先级：P2**

---

## 六、对 `/lab` 的重新定义

这一节是整份 v2 最关键的部分。

### 当前 `/lab` 是什么

当前 `main` 上的 `/lab` 更准确的定义应该是：

**一个已经具备学习闭环雏形的前端日语实验页。**

它已经有：

- 搜索
- 词卡
- 收藏
- 复习
- 自测
- 文法
- 句型

这很好，但这还不是你真正想要的那个东西。

### 你真正要的 `/lab` 是什么

你现在要的不是“更漂亮的词卡页”，而是：

**真正意义上的日语全词库系统。**

这个目标最少意味着：

1. 不再依赖客户端下载整包词库再过滤
2. 有统一词条模型
3. 有多来源导入、清洗、去重、标准化机制
4. 支持服务端搜索、分页、来源筛选、等级筛选
5. 首屏展示真实状态，而不是占位态
6. 收藏、复习、自测建立在真实词库系统之上

### 差距在哪里

当前 `main` 和这个目标之间，主要差在四层：

1. 数据层：还没外部化
2. 检索层：还没服务端化
3. 呈现层：还没完全从“学习页”转成“词库平台”
4. 工程层：主实现还没收口

---

## 七、整改优先级路线图

## P0：先把 `/lab` 架构做对

1. 为词库定义统一数据模型
2. 把词库从 `.ts` 大数组逐步迁移到更可持续的数据层
3. 建立服务端检索接口
4. 支持关键词、假名、中文、英文、来源、等级、分页查询
5. 让 `/lab` 首屏直接消费真实词库结果，而不是前端整包加载

---

## P1：把主实现收口

1. 明确首页、Projects、Lab、About、Contact、Games 的正式主实现文件
2. 清理 `V2 / Clean / Enhanced / backup` 并存问题
3. 重写 README
4. 整理根目录内容资产和日志文件
5. 给 `useStudyStore` 增加更清晰的版本化和状态分层

---

## P2：继续工程化

1. 补 `typecheck`
2. 补 `format`
3. 补最小 smoke check
4. 继续压缩超大组件
5. 拆清 `lib / data / types` 的边界

---

## 八、当前最合理的执行顺序

如果只按“现在最值钱”的顺序排，我建议是：

1. 先把 `/lab` 词库系统改成服务端检索基础架构
2. 再清理组件目录主实现
3. 再修 README 和根目录资产
4. 再做 `useStudyStore` 版本化
5. 再做大组件拆分

原因很简单：

**你真正的竞争力不在于“又多一个页面”，而在于 `/lab` 能不能从 demo 学习页跨过去，变成一个真的词库系统。**

---

## 九、最终结论

这个仓库依然是“值得继续做，不值得推倒重来”。

但和上一版评估相比，这次更明确的一点是：

**当前最重要的问题已经不是单纯的工程收口，而是 `/lab` 的产品目标已经升级，而主分支实现还没跟上。**

所以你现在最应该切换的思维不是：

- “再加一点功能”

而是：

- “先把 `/lab` 的底层架构升级到配得上‘真正全词库系统’这个目标”

一句话结论：

**MYweb 当前最该做的，不是继续扩页面，而是让 `/lab` 从“前端可用学习页”升级成“真正的日语全词库系统基础设施”，同时把主实现收干净。**
